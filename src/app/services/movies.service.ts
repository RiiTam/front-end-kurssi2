import { Injectable } from '@angular/core';
//import { parseString } from 'xml2js';
import {map} from 'rxjs/operators';

@Injectable({
  providedIn: 'root'
})
export class MoviesService {

  //url: string;
  //newCount: number;

  //testData =

    //getData(): observable<any> {
    //let params = new httpParams().set("area", "1041").set("categoryID", "1041");

    //return this.httpClient.get(this.url, options { responseType: 'text', params: params })
                     // .pipe(map(project response => {
                        //let newsData = [];

                       // parseString(response, {trim:true,
                                              // explicitArray:false
                                               //mergeAttrs:true}, function (err, result) {
                          //console.log('getJsonData')
                       // })
    //}

  }

  //constructor() { }
//}
